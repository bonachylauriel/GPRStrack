pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        mavenLocal()
        maven{url 'https://maven.google.com'}
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven { url 'https://jitpack.io'}
    }
}
rootProject.name = "GPRSTrack"
include ':app'
include ':slideDateTimePicker'
include ':hellocharts-library'
include ':hellocharts-library'
include ':slideDateTimePicker'
